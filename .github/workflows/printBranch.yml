# Created by Josue Lopez Aug 14, 2020
name: print Branch

on:
  pull-request:
    branches: [ master ]

jobs:
  print-branch-job:
      name: print branch job

      runs-on: ubuntu-latest

      strategy:
        matrix:
          node-version: [10.x]

      steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: git push release tags to GitHub
        run: |
            echo "print a branch, to find which:"
            echo ${GITHUB_REF##*/}
